[project]
name = "mini-merkle-blockchain"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.139.2",
    "itsdangerous>=2.2.0",
    "pytest>=7.0.0",
    "python-dotenv>=1.0.0",
]
